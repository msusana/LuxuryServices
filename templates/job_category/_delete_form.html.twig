<form method="post" action="{{ path('job_category_delete', {'id': jobCategory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this Job Category?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jobCategory.id) }}">
    <button type='submit'>❌</button>
</form>
