    <h3 class="text-extrabold">Your personal informations</h3>
    <div class="row">
    {{ form_start(form) }}
                  
                        <div class="col-xs-12 col-sm-6 col-md-4">
                            <div class='red-text'>{{ form_errors(form.profilPicture)}}</div>
                             <div class="file-field input-field"
                                 style="margin-top: 32px;">
                                <div class="btn btn-lg primary waves-effect waves-light" id='btnHeigt'>
                                {# {{ form_label(form.profilPicture) }} #}
                                {{ form_widget(form.profilPicture, { 'attr': {
                                    'accept' : '.pdf,.jpg,.doc,.docx,.png,.gif',
                                    'size' : '20000000', 
                                    'type' : 'file'
                                    }}) }}
                                    <span><i class="material-icons">insert_photo</i> Photo</span>
                                </div>
                                
                                {% if client.profilPicture != '' %}
                                <div class="existing-file">
                                    <a href="{{asset ('/assets/uploads/profilePicture/') ~ client.profilPicture }}" target="_blank"><i class="material-icons">&#xE24D;</i> profil_picture.jpg</a>
                                </div>
                                <div class="file-path-wrapper">
                                    <input class="file-path validate" type="text" placeholder="Upload your ID photo" readonly>
                                </div>
                                {% endif %}
                            </div>
                            </div>

                        <div class="col-xs-12 col-sm-6 col-md-8">
                            <div class="input-field">
                             {{ form_label(form.society, 'Society', {
                                'label_attr': {'class': 'active'}
                            }) }}
                             {{ form_widget(form.society) }}
                            </div>
                             <div class='red-text'>{{ form_errors(form.society)}}</div>
                    </div>
                    </div>
                        <div class="clearfix visible-sm"></div>
                        <div class="row">
                        
                        <div class="col-xs-12 col-sm-6 col-md-6">
                            <div class="input-field">
                             {{ form_label(form.activity, 'Activity') }}
                            {{ form_widget(form.activity) }}
                            </div>
                            <div class='red-text'>{{ form_errors(form.activity)}}</div>
                        </div>
                        <div class="col-xs-12 col-sm-6 col-md-6">
                            <div class="input-field">
                            {{ form_label(form.contactName, 'Contact name') }}
                            {{ form_widget(form.contactName) }}
                            </div>
                            <div class='red-text'>{{ form_errors(form.contactName)}}</div>
                        </div>
                        <div class="col-xs-12 col-sm-6">
                            <div class="input-field">
                            {{ form_label(form.status, 'Status') }}
                            {{ form_widget(form.status) }}
                            </div>
                            <div class='red-text'>{{ form_errors(form.status)}}</div>
                        </div>
                        <div class="col-xs-12 col-sm-6">
                            <div class="input-field">
                            {{ form_label(form.contactNumber, 'Contact Number') }}
                            {{ form_widget(form.contactNumber) }}
                            </div>
                            <div class='red-text'>{{ form_errors(form.contactNumber)}}</div>    
                        </div>
                        <div class="col-xs-12 col-sm-12">
                            <div class="input-field">
                            {{ form_label(form.contactEmail, 'Contact Email') }}
                            {{ form_widget(form.contactEmail) }}
                            </div>
                            <div class='red-text'>{{ form_errors(form.contactEmail)}}</div>    
                        </div>
                        <div class="col-xs-12 col-sm-12 center">
                          <button  type='submit' class="btn  gradient secondary waves-effect waves-light">
                                <span><strong>UPDATE</strong> NOW</span>
                        </button>
                        </div>
                    

                    {{ form_end(form) }}
                    </div>
                    
                    <div class="row center">
                        <div class="col s12 m6">
                            <a class="btn gradient secondary waves-effect waves-light" href="{{ path('job_offer_new',{'id': client.id})}}"><i class="fa fa-long-arrow-right"></i> Crée une offre d'emploi</a>
                        </div>
                         <div class="col s12 m6" id="offer_display_btn">
                            <button class="btn gradient secondary waves-effect waves-light" ><i class="fa fa-long-arrow-right"></i> Voir mes offres</button>
                        </div>
                    </div>
                     
                    
                    <div class="offer_display">
                        {% for job in jobOffer %}
                                <div class="portfolio-wrapper">
                                    <div class="card job-card">
                                        <div class="card-content">
                                            <span class="title">
                                                <span class="card-title">{{job.jobTitle}}</span>
                                                <span class="ref grey-text">Ref. {{job.id}}</span>
                                            </span>
                                            <div class="metas mb-20">
                                                <div class="meta">
                                                    <i class="material-icons">&#xE53E;</i>{{job.salary}}&euro;
                                                </div>
                                                <div class="meta">
                                                    <i class="material-icons">&#xE916;</i>{{ job.startingDate|date("m/d/Y") }}
                                                </div>
                                                <div class="meta">
                                                    <i class="material-icons">&#xE55F;</i>{{job.location}}
                                                </div>
                                            </div>
                                            <p class="truncated">{{job.description}}</p>
                                        </div>
                                        <div class="card-action">
                                            <a class="btn btn-md primary border waves-effect waves-dark" href="{{ path ('job_offer_show', {'id': job.id})}}">Details</a>
                                        </div>
                                    </div>
                                </div>
                            <div class="no-item"></div>
                        {% endfor %}
                    
                    </div>


<section class="section-padding gray-bg">
                <div class="container">
                    <div class="row">
                        <h3 class="text-extrabold">Your K-Yachting account</h3>
                        {{form_start(form2)}}
                            <div class="col-xs-12 col-sm-3">
                                <div class="input-field">
                                    {{ form_label(form2.email, 'Email') }}
                                    {{ form_widget(form2.email) }}
                                </div>
                            </div>
                             <div class="col-xs-12 col-sm-3">
                                <div class="input-field">
                                    {{ form_label(form2.password, 'Current Password') }}
                                    {{ form_widget(form2.password) }}
                                    <i class="fa fa-eye show-password"></i>
                                </div>
                            </div>
                            <div class="col-xs-12 col-sm-3">
                                <div class="input-field">
                                    {{ form_label(form2.newPassword.first) }}
                                    {{ form_widget(form2.newPassword.first) }}
                                </div>
                            </div>
                            <div class="col-xs-12 col-sm-3">
                                <div class="input-field">
                                    {{ form_label(form2.newPassword.second) }}  
                                   {{ form_widget(form2.newPassword.second) }}
                                </div>
                            </div>    

                            <div class="row">
                              <div class="col-xs-12 col-sm-6 col-md-4 col-md-offset-4">
                            <button type="submit" class="btn btn-block btn-lg gradient secondary waves-effect waves-light">
                                <span><strong>UPDATE</strong> NOW</span>
                            </button>
                        </div>
                            </div>  
                        {{form_end(form2)}}
                    </div>