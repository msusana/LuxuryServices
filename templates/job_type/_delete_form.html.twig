<form method="post" action="{{ path('job_type_delete', {'id': jobType.id}) }}" onsubmit="return confirm('Are you sure you want to delete this Job Type?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jobType.id) }}">
    <button type='submit'>❌</button>
</form>
