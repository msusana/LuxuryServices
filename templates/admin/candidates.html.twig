
{% for info in admin_infos.candidates %}
    <div class="card job-card">
        <div class="card-content">
            <span class="title">
                <span class="card-title"> {{(info.firstName ) ? info.firstName : 'No FirstName' }}</span>
                <span class="card-title"> {{(info.lastName) ? info.lastName : 'No LastName'}}</span><br>                                        
                <span class="ref grey-text">Ref. {{info.id}}</span>
            </span>
            <div class="row">
                <div class="mb-20">
                        <div class="col s12 m4 black-text">
                            📩 Email : {{ (info.user.email ) }}
                        </div>
                        
                        <div class="col s12 m4 black-text">
                            🗺️ Nationality : {{(info.nationality ) ? info.nationality : 'none' }}
                        </div>

                        <div class="col s12 m4 black-text">
                            🎂 BirthDate : {{(info.dateOfBirth|date('m-d-Y') ) ? info.dateOfBirth|date('m-d-Y')  : 'none' }}
                        </div>

                        <div class="col s12 m4 black-text">
                            Availability: {{(info.availability) ? '✔️' : '❎' }}
                        </div>

                        <div class="col s12 m8 black-text">
                            🗓️ Registration date: {{(info.user.dateCreated|date('m-d-Y') ) ? info.user.dateCreated|date('m-d-Y')  : 'none' }}
                        </div>
                </div>
            </div>
                    <div class="black-text">
                       </br>Adress : {{(info.adress ) ? info.adress : 'none'}}
                    </div>
                    
                    <div class="black-text">
                        Job category : {{(info.jobCategory ) ? info.jobCategory.category : 'none' }}
                    </div>
        </div>

        <div class="card-action">
            <div class="row">
                <div class="col s12 m4">
                    <a class="btn btn-md primary border waves-effect waves-dark" href="{{ path ('candidate_edit', {'id': info.id})}}">Update</a>
                </div>
                <div class="col s12 m4">
                    <form method="post" action="{{ path('candidate_delete', {'id': info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this Candidate?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info.id) }}">
                        <button class="btn waves-effect waves-light red"><i class="material-icons">&#xE872;</i> Delete</button>
                    </form>
                </div>
                 <div class="col s12 m4">
                    <a class="btn btn-md primary border waves-effect waves-dark" href="{{ path ('candidate_edit', {'id': info.id})}}">Add Notes</a>
                </div>
            </div>
        </div>
   {{ include('info_admin_candidate/_form.html.twig') }}

    </div>


{% endfor %}
<div class="no-item"></div>