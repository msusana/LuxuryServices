
 {% for offer in admin_infos.jobOffer %}
                                <div class="card-jobOffers black-text">
                                    <div class="card-content">
                                        <span class="title">
                                            <span class="card-title-admin">{{offer.jobTitle}}</span></br>
                                            <div class="ref grey-text right-align">Ref. {{offer.id}}</div>
                                            <span class="card-title"> {{(offer.client.society) ? offer.client.society : 'No Society'}}</span>
                                        </span>
                                            <div class="row mb-20">
                                                <div class="col s12 m4">
                                                📔 Contact Name: {{(offer.client.contactName ) ? offer.client.contactName : 'none'}}
                                                </div>
                                                <div class="col s12 m4">
                                                    📩 Email : {{(offer.client.contactEmail ) ? offer.client.contactEmail : 'none' }}
                                                </div>
                                                <div class="col s12 m4">
                                                    📲 Phone Number : {{(offer.client.contactNumber ) ? offer.client.contactNumber : 'none' }}
                                                </div>
                                            </div> 

                                                <div>
                                                Status : {{ (offer.client.status ) ? offer.client.status : 'none' }} 
                                                </div>

                            
                                        <div class="row mb-20">
                                            <div class="col s12 m4">
                                                Created: {{offer.dateCreated|date("m/d/Y") }}
                                            </div>
                                            <div class="col s12 m4">
                                                Starting job date: {{ offer.startingDate|date("m/d/Y") }}
                                            </div>
                                            <div class="col s12 m4">
                                               Closing job date: {{offer.closingDate|date("m/d/Y")}}
                                            </div>
                                        </div>
                                        
                                    </div>
                                    <div class="card-action">
                                        <div class="row">
                                        <div class="col s12 m6">
                                            <a class="btn btn-md primary border waves-effect waves-dark" href="{{ path ('job_offer_edit', {'id': offer.id})}}">Update</a>
                                        </div>
                                            <div class="col s12 m6">
                                                <form method="post" action="{{ path('job_offer_delete', {'id': offer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this Job Offer?');">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.id) }}">
                                                    <button class="btn waves-effect waves-light red"><i class="material-icons">&#xE872;</i> Delete</button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            
                        
                        {% endfor %}
                        <div class="no-item"></div>
            